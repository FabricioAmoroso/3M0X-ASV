# ################################
# Model: Speaker Identification with Pretrained Wav2Vec2 (XLSR-53)
# Authors: Hwidong Na & Mirco Ravanelli
# Modified for VoxCeleb1 Open-Set Fine-Tuning
# ################################

# Basic parameters

seed: 3
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]
output_folder: !ref /scratch/yadel/jobs/output/wav2vec2_xlsr_53/voxCeleb/<seed>
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt

# Data files
data_folder: /scratch/yadel/datasets/voxCeleb
data_folder_noise: /scratch/yadel/datasets/voxCeleb/pointsource_noises
data_folder_rir: /scratch/yadel/datasets/voxCeleb/RIRs
train_annotation: !ref output/prepare/voxCeleb/train.csv
valid_annotation: !ref output/prepare/voxCeleb/dev.csv
noise_annotation: !ref output/prepare/voxCeleb/noise.csv
rir_annotation: !ref output/prepare/voxCeleb/rir.csv
verification_file: data/voxCeleb/trials.txt

split_ratio: [90, 10]
skip_prep: True
ckpt_interval_minutes: 15

number_of_epochs: 8
batch_size: 32
sample_rate: 16000
sentence_len: 3.0
shuffle: True
random_chunk: True

out_n_neurons: 1211

num_workers: 8
dataloader_options:
    batch_size: !ref <batch_size>
    shuffle: True
    num_workers: !ref <num_workers>
    pin_memory: True
    drop_last: True

# Model Functions
wav2vec2_hub: /scratch/yadel/models/wav2vec2-large-xlsr-53

wav2vec2: !new:speechbrain.integrations.huggingface.Wav2Vec2
    source: !ref <wav2vec2_hub>
    save_path: !ref <save_folder>/wav2vec2_xlsr_53_checkpoint
    freeze: False                   # Set to False to fine-tune the model downstream layers
    freeze_feature_extractor: True  # Keeps the CNN frontend frozen for architectural stability

pooling: !new:speechbrain.nnet.pooling.StatisticsPooling
    return_mean: True
    return_std: True

classifier: !new:speechbrain.nnet.linear.Linear
    input_size: 2048
    n_neurons: !ref <out_n_neurons>

epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>


add_noise: !new:speechbrain.augment.time_domain.AddNoise
    csv_file: !ref <noise_annotation>
    snr_low: 0
    snr_high: 15
    noise_sample_rate: !ref <sample_rate>
    clean_sample_rate: !ref <sample_rate>
    num_workers: !ref <num_workers>

add_reverb: !new:speechbrain.augment.time_domain.AddReverb
    csv_file: !ref <rir_annotation>
    reverb_sample_rate: !ref <sample_rate>
    clean_sample_rate: !ref <sample_rate>
    num_workers: !ref <num_workers>

drop_freq: !new:speechbrain.augment.time_domain.DropFreq
    drop_freq_low: 0
    drop_freq_high: 1
    drop_freq_count_low: 1
    drop_freq_count_high: 3
    drop_freq_width: 0.05

drop_chunk: !new:speechbrain.augment.time_domain.DropChunk
    drop_length_low: 1000
    drop_length_high: 2000
    drop_count_low: 1
    drop_count_high: 5

wav_augment: !new:speechbrain.augment.augmenter.Augmenter
    parallel_augment: True
    concat_original: True
    min_augmentations: 4
    max_augmentations: 4
    augment_prob: 1.0
    augmentations: [
        !ref <add_noise>,
        !ref <add_reverb>,
        !ref <drop_freq>,
        !ref <drop_chunk>]

mean_var_norm: !new:speechbrain.processing.features.InputNormalization
    norm_type: sentence
    std_norm: False

modules:
    wav2vec2: !ref <wav2vec2>
    pooling: !ref <pooling>
    classifier: !ref <classifier>

compute_cost: !new:speechbrain.nnet.losses.LogSoftmaxWrapper
    loss_fn: !new:speechbrain.nnet.losses.AdditiveAngularMargin
        margin: 0.15
        scale: 20

opt_class: !name:torch.optim.AdamW
    lr: 0.0001          
    weight_decay: 0.01 

# Logging + checkpoints
train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>

error_stats: !name:speechbrain.utils.metric_stats.MetricStats
    metric: !name:speechbrain.nnet.losses.classification_error
        reduction: batch

checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        wav2vec2: !ref <wav2vec2>
        classifier: !ref <classifier>
        counter: !ref <epoch_counter>