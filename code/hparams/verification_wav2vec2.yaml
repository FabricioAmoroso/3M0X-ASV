# ################################
# Model: Speaker Verification Baseline (Fine-tuned Wav2Vec2)
# Authors: Hwidong Na 2020
#          Mirco Ravanelli 2020
# Modifed for Wav2Vec2 XLSR-53 Fine-Tuning
# ################################

seed: 1234
__set_seed: !apply:speechbrain.utils.seed_everything [!ref <seed>]

# Folders and train_log file
data_folder: /home/diaelhak/links/scratch/voxceleb/vox1_testall
# !PLACEHOLDER #e.g., /localscratch/voxceleb1
output_folder: results/wav2vec2_xlsr_vox1_testall_eval_direct_epoch8/1234
save_folder: !ref <output_folder>/save

# Use the following links for the official voxceleb splits:
# VoxCeleb1 (cleaned): https://www.robots.ox.ac.uk/~vgg/data/voxceleb/meta/veri_test2.txt
# VoxCeleb1-H (cleaned): https://www.robots.ox.ac.uk/~vgg/data/voxceleb/meta/list_test_hard2.txt
# VoxCeleb1-E (cleaned): https://www.robots.ox.ac.uk/~vgg/data/voxceleb/meta/list_test_all2.txt.
# VoxCeleb1-E and VoxCeleb1-H lists are drawn from the VoxCeleb1 training set.
# Therefore you cannot use any files in VoxCeleb1 for training if you are using these lists for testing.
verification_file: /home/diaelhak/links/projects/def-aravila/diaelhak/codes/jobscode/veri_test2.txt

# Path pointing to your local directory containing your trained Wav2Vec2 checkpoints
pretrain_path: /home/diaelhak/links/projects/def-aravila/diaelhak/codes/audio_ASV/repos/speechbrain/recipes/VoxCeleb/SpeakerRec/results/wav2vec2_xlsr_vox1_devall_local_aug_nounzip/1234/save/CKPT+2026-06-29+22-03-38+00

# csv files
train_data: !ref <save_folder>/train.csv
enrol_data: !ref <save_folder>/enrol.csv
test_data: !ref <save_folder>/test.csv
split_ratio: [90, 10]
skip_prep: False

batch_size: 8
score_norm: s-norm  # z-norm t-norm s-norm (uncomment to enable it)
cohort_size: 20000 # amount of imposter utterances in normalization cohort
n_train_snts: 400000 # used for normalization stats

# Dataloader options
train_dataloader_opts:
    batch_size: !ref <batch_size>

enrol_dataloader_opts:
    batch_size: !ref <batch_size>

test_dataloader_opts:
    batch_size: !ref <batch_size>

# Model Definitions
wav2vec2_hub: /home/diaelhak/links/projects/def-aravila/diaelhak/codes/audio_ASV/hf_cache/hub/models--facebook--wav2vec2-large-xlsr-53/snapshots/c3f9d884181a224a6ac87bf8885c84d1cff3384f

wav2vec2: !new:speechbrain.lobes.models.huggingface_transformers.wav2vec2.Wav2Vec2
    source: !ref <wav2vec2_hub>
    save_path: !ref <save_folder>/wav2vec2_checkpoint

pooling: !new:speechbrain.nnet.pooling.StatisticsPooling
    return_mean: True
    return_std: True

# Register modules for verification scripts to reference
modules:
    wav2vec2: !ref <wav2vec2>
    pooling: !ref <pooling>

# Pretrainer to load the locally saved model weights from the training stage
pretrainer: !new:speechbrain.utils.parameter_transfer.Pretrainer
    collect_in: !ref <save_folder>
    loadables:
        wav2vec2: !ref <wav2vec2>
    paths:
        wav2vec2: !ref <pretrain_path>/wav2vec2.ckpt
